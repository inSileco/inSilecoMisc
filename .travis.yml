language: r
warnings_are_errors: true
cache: packages

jobs:
  include:
    - stage: unit tests
      os: linux
      dist: trusty
      r: oldrel
      env: NOT_CRAN=false
    - stage: unit tests and deploy
      os: linux
      dist: xenial
      r: release
      r_packages:
        - covr
        - pkgdown
      env:
        - NOT_CRAN=true
        - R_CODECOV=true
      deploy:
        provider: script
        script: Rscript -e 'pkgdown::deploy_site_github()'
        local_dir: dev
    - stage: unit tests
      os: osx
      r: release
      env: NOT_CRAN=true
    - stage: unit tests
      os: linux
      dist: bionic
      r: devel
      env: NOT_CRAN=false


notifications:
  email:
    recipients:
      - kevin.cazelles@gmail.com
